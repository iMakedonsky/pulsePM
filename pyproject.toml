[project]
name = "project-pulsepm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alex Mashianov", email = "mashianov@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "django>=6.1",
]

[project.scripts]
project-pulsepm = "project_pulsepm:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
