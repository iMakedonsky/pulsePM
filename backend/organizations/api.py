# from ninja import Router
#
# router = Router(tags=['Organizations'])
#
#
# @router.get('workspacea', response={200: "sda"})
# def workspace_view(request, *args, **kwargs):
#     workspace = request.GET.get('workspace')
